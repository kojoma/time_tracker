<nav class="navbar navbar-expand-lg navbar-light bg-light">
  <%= link_to '[ReviewApps] TimeTracker', root_path, {:class => "navbar-brand"} %>
  <button class="navbar-toggler" type="button" data-toggle="collapse" data-target="#navbarNav" aria-controls="navbarNav" aria-expanded="false" aria-label="Toggle navigation">
    <span class="navbar-toggler-icon"></span>
  </button>
  <div class="collapse navbar-collapse" id="navbarNav">
    <ul class="navbar-nav  mr-auto">
      <li class="nav-item<%= ' active' if current_page?(root_path) %>">
        <%= link_to 'Home', root_path, {:class => "nav-link"} %>
      </li>
      <li class="nav-item<%= ' active' if current_page?(work_hours_path) %>">
        <%= link_to 'WorkHour', work_hours_path, {:class => "nav-link"} %>
      </li>
      <li class="nav-item<%= ' active' if current_page?(users_path) %>">
        <%= link_to 'User', users_path, {:class => "nav-link"} %>
      </li>
    </ul>

    <% if user_signed_in? %>
    <ul class="navbar-nav">
      <li class="nav-item dropdown">
        <a class="nav-link dropdown-toggle" href="#" id="navbarDropdownMenuLink" data-toggle="dropdown" aria-haspopup="true" aria-expanded="false">
          <%= current_user.email %>
        </a>
        <div class="dropdown-menu" aria-labelledby="navbarDropdownMenuLink">
          <%= link_to('Sign out', destroy_user_session_path, method: :delete, class: "dropdown-item") %>
        </div>
      </li>
    </ul>
    <% end %>
  </div>
</nav>
